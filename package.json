{
  "name": "generic-quicksuite-sample",
  "version": "1.0.0",
  "description": "Generic QuickSight embedding sample application",
  "private": true,
  "scripts": {
    "test": "jest",
    "test:watch": "jest --watch"
  },
  "jest": {
    "testEnvironment": "jsdom",
    "testPathIgnorePatterns": [
      "/node_modules/",
      "/.aws-sam/"
    ]
  },
  "devDependencies": {
    "@aws-sdk/client-dynamodb": "^3.1056.0",
    "@aws-sdk/lib-dynamodb": "^3.1056.0",
    "ajv": "^8.20.0",
    "ajv-formats": "^3.0.1",
    "fast-check": "^3.15.0",
    "jest": "^29.7.0",
    "jest-environment-jsdom": "^29.7.0"
  },
  "license": "Apache-2.0"
}
